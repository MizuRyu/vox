# Security

## データの扱い

Vox は初回の音声モデル取得後、Apple の音声認識をローカルで使います。確定本文は端末内の履歴に保存します。診断ログには本文を既定で出しません。`--log-text` を明示した場合だけ確定本文を出します。

設定、履歴、ログとその権限、削除方法は [開発手順](docs/development.md#データの場所) を参照してください。履歴とログは別の機能です。設定や本文をクラウドへ送るテレメトリ機能はありません。ベンチ用データやモデルのダウンロードは、それぞれの明示的なコマンドで行います。

ファイルのプレビューは検索対象内の通常ファイルを上限付きで読みます。対象外のパス・特殊ファイルは本文を開きません。貼り付けは入力先を再確認してから行いますが、OS のクリップボード受領通知は相手アプリの挿入・送信完了を保証しません。

## 脆弱性の報告

GitHub の Security タブから **Private vulnerability reporting** で報告してください。公開 Issue に実発話、履歴、トークン、個人情報を貼らないでください。

報告には影響するバージョン、macOS の版、合成データでの再現手順、想定と実際の動作を含めてください。

## 検査の範囲

`just check` は作業ツリー・ステージ内容・存在する Git 履歴を Gitleaks とローカル Semgrep ルールで検査し、SwiftLint と ShellCheck を実行します。Swift の全データフローやすべての依存関係の脆弱性を網羅するものではありません。安全性の判断ではコードレビューと対象アプリでの動作確認も必要です。
